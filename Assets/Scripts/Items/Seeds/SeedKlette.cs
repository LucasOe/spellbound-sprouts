using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SeedKlette : Seed
{
    // Start is called before the first frame update
    void Start()
    {
        this.startAmount = 2; 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
